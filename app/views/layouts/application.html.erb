<!DOCTYPE html>
<html>
  <head>
    <title>SearchApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
        <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

<%#  検索結果を読み込むジャバスクリプト %>
 <script>
  function renderSearchResults(searchQuery) {
    var cx = 'a15bfb6d263eb4a1c'; // Google CSEのID
    var gcse = document.createElement('script');
    gcse.type = 'text/javascript';
    gcse.async = true;
    gcse.src = 'https://cse.google.com/cse.js?cx=' + cx;
    var s = document.getElementsByTagName('script')[0];
    s.parentNode.insertBefore(gcse, s);

    gcse.onload = function() {
      google.search.cse.element.init({
        div: 'search-list',
        tag: 'searchresults-only',
        attributes: {
          linkTarget: '_blank'
        }
      });

      var searchControl = new google.search.cse.element.CSESearchControl();
      searchControl.execute(searchQuery);
    };
  }
</script>

 
  </head>

  <body>
  <p>search app!
  </p>
    <%= yield %>
  </body>
</html>
